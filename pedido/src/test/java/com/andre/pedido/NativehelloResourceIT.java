package com.andre.pedido;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativehelloResourceIT extends helloResourceTest {

    // Execute the same tests but in native mode.
}